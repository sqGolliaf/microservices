spring:
  application:
    name: config-server
  profiles:
    active: native

server:
  port: 8071

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      show-values: always